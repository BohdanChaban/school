= form_with(model: hometask, local: true) do |form|
  - if hometask.errors.any?
    #error_explanation
      %h2
        = pluralize(@hometask.errors.count, "error")
        prohibited this hometask from being saved:
      %ul
        - hometask.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :description
    = form.text_field :description
  .field
    = form.label :link
    = form.text_field :link
  .field
    = form.label :lesson_id
    = form.select :lesson_id,                                    |
      options_from_collection_for_select(lessons, 'id', 'topic')
  .actions
    = form.submit
